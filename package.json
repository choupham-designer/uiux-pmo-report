{
  "name": "uiux-pmo-report",
  "version": "1.0.0",
  "description": "UI/UX PMO Weekly Report",
  "private": true,
  "scripts": {
    "dev": "npx serve .",
    "build": "echo 'Static HTML - no build required'",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {
    "@vercel/analytics": "^1.4.1"
  }
}
